master.listening.ip = <master's ip address>
master.listening.port.client = <master's listening port for clients>
master.listening.port.server = <master's listening port for servers>
master.clients = <number of clients>
master.servers = <number of servers>

pod.network.interface = <network interface>

#Controller benchmark strategy
controller.benchmark.strategy = <benchmark strategy class implementing IBenchmarkStrategy>

master.rounds = 1 10 20 40 80 100 500 1000 1500 2500 3000

global.working.directory = .
global.clients.maxPerWorker = 30

#[true|false]
global.isWrite = true

#In bytes
global.data.size = 1024